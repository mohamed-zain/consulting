function validation() {

    if (document.getElementById("NameAR")) {

        var number = document.getElementById("NameAR").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الاسم كاملا باللغة العربية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

        if (document.getElementById("NameEN")) {

        var number = document.getElementById("NameEN").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الاسم كاملا باللغة الانجليزية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }



      if (document.getElementById("Email")) {

        var number = document.getElementById("Email").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل البريد الالكتروني</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("Nationality")) {

        var number = document.getElementById("Nationality").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الجنسية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

        if (document.getElementById("Qualify")) {

        var number = document.getElementById("Qualify").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل المؤهل الاكاديمي</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
     if (document.getElementById("BirthPlace")) {

        var number = document.getElementById("BirthPlace").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل مكان الميلاد</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("BirthDate")) {

        var number = document.getElementById("BirthDate").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل تاريخ الميلاد</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("MobPhone")) {

        var number = document.getElementById("MobPhone").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل هاتف الموظف</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

      if (document.getElementById("HomePhone")) {

        var number = document.getElementById("HomePhone").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل هاتف المنزل</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("City")) {

        var number = document.getElementById("City").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل المدينة</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("Quarter")) {

        var number = document.getElementById("Quarter").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل المحلية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
     if (document.getElementById("Branch")) {

        var number = document.getElementById("Branch").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الفرع</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
     if (document.getElementById("AssignDate")) {

        var number = document.getElementById("AssignDate").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل تاريخ التعين</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("AssignType")) {

        var number = document.getElementById("AssignType").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل نوع التعين</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("AssignType")) {

        var number = document.getElementById("AssignType").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل نوع التعين</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("WorkHours")) {

        var number = document.getElementById("WorkHours").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل ساعات العمل</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("JobTitle")) {

        var number = document.getElementById("JobTitle").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل المسمي الوظيفي</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

       if (document.getElementById("JobDegree")) {

        var number = document.getElementById("JobDegree").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الدرجة الوظيفية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("Managment")) {

        var number = document.getElementById("Managment").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الادارة</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
    if (document.getElementById("MainDirector")) {

        var number = document.getElementById("MainDirector").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل المدير المباشر</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

      if (document.getElementById("JobName")) {

        var number = document.getElementById("JobName").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الوصف الوظيفي </div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

      if (document.getElementById("MainSalary")) {

        var number = document.getElementById("MainSalary").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الراتب الاساسي  </div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("LivePremium")) {

        var number = document.getElementById("LivePremium").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل بدل السكن</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("TransPremium")) {

        var number = document.getElementById("TransPremium").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل بدل النقل</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("BankNamemm")) {

        var number = document.getElementById("BankNamemm").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل اسم البنك</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

      if (document.getElementById("BankBranch")) {

        var number = document.getElementById("BankBranch").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل فرع البنك</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

      if (document.getElementById("BankAccount")) {

        var number = document.getElementById("BankAccount").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل رقم الحساب</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("IdentityID")) {

        var number = document.getElementById("IdentityID").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل الهوية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("IdentitySrc")) {

        var number = document.getElementById("IdentitySrc").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل مصدر الهوية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("IdentityDate")) {

        var number = document.getElementById("IdentityDate").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل تاريخ اصدار الهوية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }


    if (document.getElementById("IdenDateEnd")) {

        var number = document.getElementById("IdenDateEnd").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل تاريخ انتهاء الهوية</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }


    if (document.getElementById("PassportID")) {

        var number = document.getElementById("PassportID").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل رقم الجواز</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("PassportSrc")) {

        var number = document.getElementById("PassportSrc").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل مصدر الجواز</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("PassportDate")) {

        var number = document.getElementById("PassportDate").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل تاريخ اصدار الجواز</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("PasDateEnd")) {

        var number = document.getElementById("PasDateEnd").value;


        if (number == "") {

            text = '<div class="notify bottom-left do-show" data-notification-status="error">الرجاء ملئ حقل تاريخ انتهاء الجواز</div>';
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }







    if (document.getElementById("AgentType")) {

        var number = document.getElementById("AgentType").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء اختيار نوع العميل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("AgentName")) {

        var number = document.getElementById("AgentName").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة اسم العميل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("AgentNation")) {

        var number = document.getElementById("AgentNation").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة جنسية العميل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

        if (document.getElementById("Agentauth")) {

        var number = document.getElementById("Agentauth").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة رقمة هوية العميل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }


        if (document.getElementById("AgentMob")) {

        var number = document.getElementById("AgentMob").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة هاتف  العميل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }


        


        if (document.getElementById("ProDesc")) {

        var number = document.getElementById("ProDesc").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة وصف للمشروع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

        if (document.getElementById("ProManager")) {

        var number = document.getElementById("ProManager").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء اختيار مدير المشروع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("AgenName")) {

        var number = document.getElementById("AgenName").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء اختيار اسم العميل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
    if (document.getElementById("Price")) {

        var number = document.getElementById("Price").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة تكلفة المشروع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

       if (document.getElementById("Cash")) {

        var number = document.getElementById("Cash").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة المبلغ المدفوع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
    if (document.getElementById("teams")) {

        var number = document.getElementById("teams").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء اختيار فريق العمل!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }


    if (document.getElementById("ProType")) {

        var number = document.getElementById("ProType").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء اختيار نوع المشروع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     if (document.getElementById("BranchID")) {

        var number = document.getElementById("BranchID").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء اختيار الفرع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
 
    

    if (document.getElementById("ProName")) {

        var number = document.getElementById("ProName").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة اسم المشروع!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }
/*******************************سجل الخصوز*******************************************/



    if (document.getElementById("OppoName")) {

        var number = document.getElementById("OppoName").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة اسم الخصم!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

    if (document.getElementById("OppoNation")) {

        var number = document.getElementById("OppoNation").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة جنسية الخصم!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }

     


        if (document.getElementById("OppoMob")) {

        var number = document.getElementById("OppoMob").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة هاتف  الخصم!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }


        if (document.getElementById("OppoAddress")) {

        var number = document.getElementById("OppoAddress").value;


        if (number == "") {

            text =swal("خطأ", "الرجاء كتابة عنوان  الخصم!", "error");
            document.getElementById("MSG").innerHTML = text;
            
            return false;

        }

    }



/*******************************سجل الخصوز*******************************************/


/**********************************************************************************/

    return true;

}





